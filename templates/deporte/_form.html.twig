{{ form_start(form) }}
    {{ form_widget(form) }}
    {% if 'editar' in app.request.uri %}
        <input type="hidden" name="oldName" value="{{ deporte.nombre }}">
    {% endif %}
<button class="btn btn-primary mt-2">
    {{ button_label|default('Guardar') }}
</button>
{{ form_end(form) }}
